{
  /* <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
  <Card className="hover:shadow-lg transition-shadow">
    <CardHeader className="flex flex-row items-center justify-between pb-2">
      <CardTitle className="text-sm font-medium text-gray-500">
        Luas Keseluruhan Lahan
      </CardTitle>
      <MapPin className="w-4 h-4 text-blue-500" />
    </CardHeader>
    <CardContent>
      <div className="text-2xl font-bold mb-1">
        {stats.totalArea.toLocaleString("id-ID", {
          maximumFractionDigits: 2,
        })}
      </div>
      <p className="text-xs text-gray-500">Total lahan di seluruh wilayah</p>
    </CardContent>
  </Card>

  <Card className="hover:shadow-lg transition-shadow">
    <CardHeader className="flex flex-row items-center justify-between pb-2">
      <CardTitle className="text-sm font-medium text-gray-500">
        Target Monokultur
      </CardTitle>
      <Target className="w-4 h-4 text-green-500" />
    </CardHeader>
    <CardContent>
      <div className="text-2xl font-bold mb-1">
        {stats.monokulturTarget.toLocaleString("id-ID", {
          maximumFractionDigits: 2,
        })}
      </div>
      <p className="text-xs text-gray-500">
        2% dari total lahan di seluruh wilayah
      </p>
    </CardContent>
  </Card>

  <Card className="hover:shadow-lg transition-shadow">
    <CardHeader className="flex flex-row items-center justify-between pb-2">
      <CardTitle className="text-sm font-medium text-gray-500">
        Target Tumpang Sari
      </CardTitle>
      <Target className="w-4 h-4 text-purple-500" />
    </CardHeader>
    <CardContent>
      <div className="text-2xl font-bold mb-1">
        {stats.tumpangSariTarget.toLocaleString("id-ID", {
          maximumFractionDigits: 2,
        })}
      </div>
      <p className="text-xs text-gray-500">
        7% dari total lahan di seluruh wilayah
      </p>
    </CardContent>
  </Card>

  <Card className="hover:shadow-lg transition-shadow">
    <CardHeader className="flex flex-row items-center justify-between pb-2">
      <CardTitle className="text-sm font-medium text-gray-500">
        Total Target
      </CardTitle>
      <TargetIcon className="w-4 h-4 text-orange-500" />
    </CardHeader>
    <CardContent>
      <div className="text-2xl font-bold mb-1">
        {stats.totalTarget.toLocaleString("id-ID", {
          maximumFractionDigits: 2,
        })}
      </div>
      <p className="text-xs text-gray-500">2% monokultur + 7% tumpang sari</p>
    </CardContent>
  </Card>
</div>; */
}
